# Generated by chezmoi on first init. Tweak per machine after bootstrap.

sourceDir = "{{ .chezmoi.homeDir }}/.dotfiles"

[data]
    machine = "{{ .chezmoi.hostname }}"
    role = "{{ promptStringOnce . "role" "Machine role (personal/work/remote)" }}"
    use_starship = true
    use_atuin = true
